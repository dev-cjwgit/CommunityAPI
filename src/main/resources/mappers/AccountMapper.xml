<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="repository.AccountMapper">
    <insert id="signUp">
        <!-- db 구조 -->
        INSERT INTO account(`email`, `password`, `name`, `nickname`)
        VALUES (#{account.email}, #{account.password}, #{account.name}, #{account.nickname})
    </insert>

    <update id="setSalt">
        UPDATE account
        SET salt = #{salt}
        WHERE uid = #{uid}
    </update>

    <select id="isAccountToEmail" resultType="short">
        SELECT count(*)
        FROM account
        WHERE email = #{email}
    </select>
    <select id="isAccountToNickName" resultType="short">
        SELECT count(*)
        FROM account
        WHERE nickname = #{nickname}
    </select>

    <select id="getUidToEmail" resultType="Long">
        SELECT uid
        FROM account
        WHERE email = #{email}
    </select>

    <select id="getPasswordToEmail" resultType="string">
        SELECT password
        FROM account
        WHERE email = #{email}
    </select>

    <select id="getSaltToUid" resultType="string">
        SELECT salt
        FROM account
        WHERE uid = #{uid}
    </select>

    <select id="getAccountToEmail" resultType="domain.AccountDTO">
        SELECT *
        FROM account
        WHERE email = #{email}
    </select>

</mapper>